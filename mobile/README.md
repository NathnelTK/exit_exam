# Exit Planner Mobile (React Native / Expo)\n\n## Run\n\n```bash\ncd mobile\nnpm install\nnpm run start\n```\n\n- Android: `npm run android` (requires Android SDK)\n- iOS: `npm run ios` (on macOS with Xcode)\n\nThe app expects the backend at `http://localhost:5000`. On a device/emulator, adjust to your machine IP.\n\n## Screens\n- Dashboard\n- Settings\n- Uploads (PDF/images)\n- Weekly Plan (generate)\n- Mock Exam (generate)\n\n## Notes\n- Uploads use multipart/form-data to `/api/materials`.\n- Weekly plan calls `/api/weekly-plans/generate`.\n- Mock exam calls `/api/mock-exams/generate`.\n*** End Patch```"?> ***!

